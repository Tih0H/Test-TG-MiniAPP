import styles from "../Styles/Pagewebsocket.css";

const Pagewebsocket = () => {
	return (
		<>
			<h1 className={styles.container}>
				на одном экране будет работать с websocket{" "}
			</h1>
		</>
	);
};

export default Pagewebsocket;
